spring:
  profiles:
    active: dev
  application:
    name: group_project
server:
  port: 8070
logging:
  level:
    com:
      dikshanta:
        restaurant:
          management:
            system:
              group_project: DEBUG
utils:
  admin:
    name: ${ADMIN_NAME:Admin User}
    email: ${ADMIN_EMAIL:admin@example.com}
    password: ${ADMIN_PASSWORD:ChangeMe123!}
    profileImagePath: "uploads/user/dummy_profile.png"
  jwt:
    secret: ${JWT_SECRET:CHANGE_ME_WITH_LONG_RANDOM_SECRET}
    expiry: ${JWT_EXPIRY_MINUTES:10}
file:
  upload-dir: "uploads/"


